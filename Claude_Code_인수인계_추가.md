# Claude Code 인수인계 — 추가 사항 (2026-05-04 추가)

기존 `Claude_Code_인수인계.md`에 누적 반영. 본 문서는 변경된 부분만 정리.

---

## 1. vibe 시스템 변경 — 5개 → **8개 카테고리**

### 기존 인수인계 메모와 다른 점
기존 메모 §5에는 vibe 5개 (조용함/번화함/청년활기/가족친화/자연친화)로 적혀있음.
**8개로 확장 결정** (자연어 흡수율 향상 목적):

```
1. 조용함
2. 번화함
3. 청년활기
4. 가족친화
5. 자연친화
6. 편의 우선        ← 신규
7. 운동·건강       ← 신규
8. 카페·문화       ← 신규
```

### 데이터 소스 단순화 (옵션 A 채택)
- **카카오 POI만 사용** (생활인구·인구통계·녹지율 통합은 마감 후로 연기)
- 기존 인수인계 메모 §5 표의 "생활인구", "인구통계", "도시계획 데이터" 컬럼 폐기
- v5 인프라 7종 (편의점·병원·약국·마트·카페·헬스장·공원)으로만 측정

### vibe 가중치 처리 변경
- 기존 메모: 가중치 임의값 (0.5, 1.5 등)
- **변경**: 1~5 정수 척도 평가 후 정규화 (학술적 정당성 보강)
- 자세한 표는 `vibe_매핑_설계.md` §6 참고

---

## 2. 챗봇 흐름 — 청년정책 단계 추가, 인프라 단계 조건부

### 기존 슬롯 정의
필수 5개: 직장주소, 이동수단, 예산, 통근시간, 주택유형
선택: 지역, 우선순위, vibe, 청년정책

### 변경된 챗봇 진행 순서

```
1. 직장 주소 (필수)
2. 이동수단 (필수)
3. 예산 (필수)
4. 통근시간 (필수)
5. 주택유형 (필수)
6. TOPSIS 4축 가중치 (필수)
   - 통근 우선 / 주거비 우선 / 균형 / 직접 입력 / 알아서 해줘
7. 인프라 반영 여부 + vibe 선택
   - 첫 입력에 vibe 표현 있으면: 건너뛰기 (자동 ON)
   - 없으면: "동네 분위기 반영하시겠어요?" 8개 + 균등 옵션
8. 청년정책 반영 여부 (필수, 항상 묻기)
   - 표현: "현재 조건에 맞는 청년정책이 있는데 같이 확인하시겠어요?"
   - 사용자가 "청년인데~"라고 자기소개하지 않음. 키워드 자동 추출 비현실적
9. 확인 단계 → 추천 실행
```

### 핵심 변경사항
- **청년정책은 키워드 자동 추출 X** → 항상 묻기로 변경
- vibe만 자연어 자동 추출 (예: "조용한 곳" 등 표현 빈도 높음)

---

## 3. 가중치 시스템 — 두 층위 분리

### 사용자가 직접 결정 (1층)
- **TOPSIS 4축 가중치** (통근:주거비:인프라:정책)
- 챗봇이 명시적으로 질문, 사용자가 비율 선택
- 학술적 정당성: 사용자 선호 반영 = TOPSIS 본래 의도

### 시스템이 자동 결정 (2층, 사용자 위임)
- **인프라 7종 내부 가중치** (편의점/병원/...)
- vibe 카테고리 선택에 따라 자동 적용
- 사용자가 "알아서 해줘" 선택 시 균등 가중치 (1.0)
- 학술적 정당성: 사용자가 결정 위임 = 동의된 기본값

---

## 4. 매칭 실패 처리 (vibe)

8개 카테고리에 매칭 안 되는 표현(예: "역세권", "학세권", "신축") 처리:

```
LLM 출력: vibe=null, unrecognized="역세권"
   ↓
챗봇:
"'역세권'은 통근시간으로 반영하실 수 있어요.
 분위기는 어떤 걸 선호하세요?
 ① 조용한 ② 번화한 ③ 그냥 진행 (분위기 미반영)"
```

→ 사용자가 다시 선택. "이상한 자동 매핑" 방지.

---

## 5. 슬롯 스키마 (확정 버전)

```python
SLOT_SCHEMA = {
    # 필수 슬롯
    "work_address": str,
    "transport_mode": str,             # "car" | "transit"
    "budget": {
        "rent_type": str,              # "전세" | "월세"
        "deposit_manwon": int,
        "monthly_manwon": int,
    },
    "allowed_minutes": int,
    "house_type": str,                 # "연립다세대" | "오피스텔" | None(둘다)
    "weight_preference": str,          # "통근우선" | "주거비우선" | "균형" | "위임"
    "use_youth_policy": bool,          # 청년정책 반영 여부 (항상 묻기)
    
    # 선택 슬롯
    "region_filter": str,              # 지역 필터 (예: "강남구")
    "depart_time": str,                # "HHMM"
    "arrive_time": str,                # "HHMM"
    "vibe": list[str] | None,          # 8개 카테고리 중 다중 가능
    
    # 메타
    "vibe_unrecognized": str | None,   # 매칭 실패 표현 보존
}
```

---

## 6. 작업 우선순위 (마감 5/13 기준)

### Phase 5 (vibe 학술 근거 메모) — 완료
- `vibe_매핑_설계.md` 작성 완료
- Jacobs(1961) + KCI 논문 + AHP-TOPSIS 인용 확보

### Phase 6 (Streamlit + 챗봇 개발)

#### Phase 6-1: nlp_input_module.py 신규
- 더미 슬롯 추출기 (정규식 기반)
- 챗봇 상태 머신 (한 번에 1개씩 역질문)
- vibe 자동 추출 + 매칭 실패 시 폴백
- 무한 루프 방지 장치 (MAX_TURNS=7)

#### Phase 6-2: vibe_module.py 신규
- VIBE_RAW_SCORES 표 (1~5 척도)
- normalize_weights() 함수
- get_vibe_weights(vibe_list) → 인프라 가중치 dict
- 다중 vibe 평균 처리

#### Phase 6-3: streamlit_app.py 신규
- 좌측: 챗봇 대화창
- 우측: 슬롯 추출 상태 + 추천 결과
- 디버깅 패널: vibe 매칭 결과 / 가중치 dict 실시간 표시

#### Phase 6-4 (선택): 청년정책 모듈 개선
- 별도 메모 `청년정책_시스템_개선계획.md` 참고
- 마감 안에 모두 반영은 어려움. 우선순위 보고 진행

---

## 7. 발표 메시지 (vibe 부분)

> "동네 분위기 선호는 사용자 자연어 입력에서 LLM이 8개 카테고리(조용함/번화함/청년활기/가족친화/자연친화/편의 우선/운동·건강/카페·문화)로 자동 분류. 매칭 실패 표현은 사용자에게 재질문하여 잘못된 자동 매핑 방지. 카테고리별 인프라 가중치는 Jacobs(1961) 도시활력 이론과 AHP-TOPSIS 방법론 참고하여 1~5점 척도 평가, 향후 사용자 피드백 누적 시 정식 AHP 보정 예정."

---

## 8. 다음 작업 시작점

Claude Code에서:
```
다음 메모들을 순서대로 읽고 Phase 6-1부터 진행:
1. Claude_Code_인수인계.md (기존)
2. Claude_Code_인수인계_추가.md (이 파일)
3. vibe_매핑_설계.md
4. 청년정책_시스템_개선계획.md
5. 회원가입_설계.md

각 작업 단위 완료 후 사용자에게 검증 요청.
큰 변경(파일 삭제/함수 제거)은 적용 전 미리 확인받기.
```

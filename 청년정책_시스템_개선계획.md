# 청년정책 시스템 개선 계획 (Phase 6 작업 지시서)

작성일: 2026-05-04
대상 모듈: `youth_policy_module.py` (585줄)
적용 시점: Phase 6 (Claude Code에서 진행)

---

## 1. 현재 상태 진단

### 1-1. 사용자에게 받는 정보 (3개만)
- 나이
- 취업 상태
- 서울 거주 여부

### 1-2. 정책 매칭 흐름 (4단계)
1. **1차 필터**: 정책 텍스트에서 정규식으로 보조금 추출
2. **2차 필터**: 기본 자격(서울+나이+취업)만 검증
3. **3차 검증**: 사용자에게 "조건 충족합니까? y/n/잘모름" 자가 응답
4. **4차 점수**: 충족 정책 상위 3개 절감액 합산 → 점수

### 1-3. 정책 자격 데이터 — API가 이미 다 가져옴
| 필드 | 의미 | 활용 상태 |
|---|---|---|
| `sprtTrgtMinAge` / `sprtTrgtMaxAge` | 지원 나이 | 자동 매칭 ✅ |
| `mrgSttsCd` | 혼인상태 | 출력만, 매칭 안 함 |
| `earnCndSeCd` / `earnMaxAmt` | 소득조건 | 출력만, 매칭 안 함 |
| `schoolCd` | 학력 | 출력만, 매칭 안 함 |
| `addAplyQlfcCndCn` | 추가 자격 (자유 텍스트) | 출력만 |
| `bizPrdBgngYmd` / `bizPrdEndYmd` | 사업 기간 | 출력만 |

→ **자격 데이터는 풍부하나, 시스템이 자동 매칭하지 않고 사용자 자가 응답에 의존하는 구조.**

### 1-4. 현재 산식의 문제

| 산식 | 현재 | 문제 |
|---|---|---|
| `보증금 / 200` | 환산보증금 곱셈수 200 고정 | 전환율 6% 가정. 법정 산식과 불일치 |
| `최대지원금 × 0.5 / 12` | 0.5 (50%) 곱셈 | 임의값. 근거 없음 |
| `(100 - 할인율) / 100 × 50` | 월 50만원 기준 환산 | 임의값. 50만원 기준 근거 없음 |
| `default 8` (전세대출) / `15` (월세지원) | 임의 디폴트값 | 임의값 |
| `점수 = 절감액 / 50 × 100` | 50만원 = 100점 | 임의값. 학술/공공 표준 없음 |

---

## 2. 개선 계획

### 2-1. 자격 검증 흐름 재설계 (사용자 제안 반영)

**변경 흐름:**
```
[1] 동네 정책 목록 가져오기 (API 호출)
    ↓
[2] 공통 자격 정보 사용자 입력 (1회)
    - 필수: 나이, 취업상태, 거주지
    - 추가: 소득(연소득 또는 월소득), 자산, 혼인상태, 무주택 여부, 학력
    ↓
[3] 시스템 자동 매칭 (공통 자격)
    - 나이 ✅ (이미 구현)
    - 취업 ✅ (이미 구현)
    - 소득: earnMaxAmt와 사용자 소득 비교
    - 혼인: mrgSttsCd와 사용자 혼인상태 비교
    - 학력: schoolCd와 사용자 학력 비교
    ↓
[4] 매칭 통과 정책 후보 표시
    "이 동네에서 받을 수 있는 정책 N개 발견"
    ↓
[5] 사용자가 자세히 볼 정책 선택
    ↓
[6] 선택한 정책의 개별 자격(addAplyQlfcCndCn 등) 표시
    → 사용자 자가 확인 (현재 방식 유지)
    ↓
[7] 확정된 정책으로 점수 계산
```

**구현 변경점:**
- `collect_youth_info()` 확장: 소득/자산/혼인/무주택/학력 추가 입력
- `_check_income()`, `_check_marriage()`, `_check_education()` 함수 신규 추가
- 자격 체크 결과 표시: "✅ 자동 매칭 통과 / ⚠ 사용자 확인 필요"

---

### 2-2. 전월세 전환율 법정 산식 적용 (근거 명확)

**근거 자료:**
- 주택임대차보호법 제7조의2 (월차임 전환 시 산정률의 제한)
- 동법 시행령 제9조: MIN[한국은행 기준금리 + 2%, 10%]
- 한국부동산원 R-ONE 자치구별 전월세 전환율 통계 (월간 공표)
- 출처: https://stat.kosis.kr (한국부동산원 부동산통계)
- 출처: https://adrhome.reb.or.kr (전월세전환 계산기)

**현재 → 변경:**
```python
# 현재 (보증금 → 월 환산)
monthly_equiv = deposit / 200    # 임의 곱셈수

# 변경 (법정 산식 적용)
# 보증금 × 전환율(연) ÷ 12 = 월 환산액
monthly_equiv = deposit * conversion_rate / 12

# 전환율 출처 우선순위:
# 1순위: 한국부동산원 자치구별 실측 통계 (월간 갱신)
# 2순위: 법정 상한 (기준금리 + 2%)
# 3순위: 폴백 — 6% (현재값과 동일)
```

**구현 변경점:**
- `youth_policy_module.py`에 `conversion_rate_lookup()` 함수 추가
- 한국부동산원 통계 CSV 또는 API 활용 (자치구 단위)
- 법정 상한 검증 후 적용
- 데이터 부재 시 6% 폴백 + 경고 표시

---

### 2-3. 정책 유형별 산식 분리 + 솔직한 근거 명시

**근거가 명확한 것:**
- **월세 직접지원**: `월 지원금 × 지원개월수`는 정책 명시값 그대로 사용 (근거 명확)
- **전세대출 이자지원**: `대출한도 × (시중금리 - 정책금리) ÷ 12` (한국은행 기준금리 사용 가능)
- **시세 할인 임대**: `해당 동 평균 시세 × 할인율` (주거비 CSV 활용)

**근거가 없는 것 (임의 산식):**
- 일회성 지원금의 월 환산 (몇 개월로 나눌지 임의)
- 보증금 무이자 대출의 절감액 (기회비용 계산법 다양)
- 서로 다른 유형 정책의 점수 단일화

**처리 방식:**
1. 근거 명확한 산식은 **수식 + 근거 출처 코드 주석에 명시**
2. 근거 없는 산식은 **"추정 산식" 표기 + 사용자에게 신청 URL에서 직접 확인 안내**
3. 모든 산식의 가정값(예: 거주 12개월 가정)은 **명시적 상수로 분리**

**예시 변경:**
```python
# ────────────────────────────────────────────────────
# 정책 유형별 보조금 환산 산식
#
# 근거 명확한 산식:
#   월세지원: 정책 명시 월 지원금 그대로 (근거: 정책 공고문)
#   전세대출 이자지원: 한국은행 기준금리 - 정책금리 (근거: 한은 기준금리)
#
# 추정 산식 (학술/공공 표준 없음, 임의 가정):
#   일회성 지원: 12개월 균등 분할 가정
#   보증금 무이자 대출: 기회비용 = 대출액 × 4.5% / 12 가정
#
# ⚠ 추정 산식은 절대값 의미 약함. 동네 간 상대 비교용.
#   사용자는 정책 신청 URL에서 정확한 혜택 확인 필요.
# ────────────────────────────────────────────────────

ASSUMED_RESIDENCE_MONTHS = 12      # 거주기간 가정 (추정)
ASSUMED_OPPORTUNITY_RATE = 0.045   # 기회비용 금리 (한국은행 기준 + 적정 가산)
```

---

### 2-4. 점수 산식 — "절대값 → 상대 순위" 포지셔닝 변경

**현재 문제:**
- "월 50만원 절감 = 100점"이라는 기준 자체가 임의
- 사용자별 시세/예산이 달라 절대값 비교 부적절

**변경:**

방안 A. **사용자 예산 대비 비율 점수**
```python
# 사용자 입력 예산 활용
ratio = total_monthly_saving / user_budget_monthly
score = min(100, ratio * 100)
# → "내 예산 대비 얼마나 절감되는가" 정량화
# → 사용자에게 의미 있는 점수
```

방안 B. **상대 순위 표시 (절대값 점수 폐지)**
```
강남구 역삼동: 청년정책 4건 매칭, 월 35만원 절감 추정 (이 동네 추천 매물 중 상대 1순위)
강남구 삼성동: 청년정책 3건 매칭, 월 22만원 절감 추정 (상대 4순위)
```

**권장: 방안 A 적용 + 결과 화면에 한계 명시**

근거:
- 사용자 예산 대비 비율은 의미 명확 (사용자 입장 정량화)
- TOPSIS 4축 통합 시에도 정규화된 0~1 값 사용
- "추정 산식이므로 절대값보다 동네 간 상대 비교 권장" 안내 추가

---

### 2-5. 중복 수혜 처리

**현재 문제:**
- 온통청년 API에 "중복 수혜 가능 여부" 필드 없음
- 자동 매칭 불가능

**처리 방식:**

1. **텍스트 키워드 검색 + 경고**
```python
DUP_LIMIT_KEYWORDS = [
    "중복 불가", "중복불가", "중복 신청 불가",
    "타 사업과", "유사 사업", "동일 사업",
    "중복 수혜 제한", "중복지원 제한",
]

def detect_dup_limit(policy: dict) -> bool:
    text = (policy.get("addAplyQlfcCndCn", "") +
            policy.get("plcySprtCn", "") +
            policy.get("plcyExplnCn", ""))
    return any(kw in text for kw in DUP_LIMIT_KEYWORDS)
```

2. **결과 화면에 경고 표시**
```
⚠ 다음 정책은 중복 수혜 제한 가능성 있음:
  - 서울 청년월세 지원
  - 청년 주거안정 자금 대출

   각 정책 신청 URL에서 중복 가능 여부 확인 필요.
```

3. **마지막 안내**
```
※ 본 시스템의 보조금 절감액은 추정값입니다.
   중복 수혜 가능 여부, 정확한 자격, 신청 가능 여부는
   각 정책의 신청 URL에서 반드시 확인하세요.
```

---

### 2-6. 추천 근거 화면 출력

**현재 출력 문제:**
- "청년정책 점수 50점"만 표시 → 왜 50점인지 사용자가 모름

**변경 출력 예시:**
```
[강남구 역삼동] 청년정책 점수 65/100 (사용자 예산 대비 환산)

  매칭 근거:
  ─────────────────────────────────
  ① 서울시 청년월세 지원 [확정 매칭]
     자격 검증:
       만 19~39세  → 사용자 28세 ✓
       무주택      → ✓
       소득 250만원 이하 → 사용자 220만원 ✓
     혜택:
       월 20만원 × 12개월 = 240만원
     산출 근거: 정책 공고문 명시 금액 (신뢰도 ★★★)

  ② 행복주택 [부분 매칭]
     자격 검증:
       청년 자격 → ✓
       자산 기준 → ⚠ 사용자 자가 응답 (불확실)
     혜택:
       시세 80% 임대 → 월 약 12만원 절감 추정
     산출 근거: 해당 동 평균 시세(60만원) × 20% 할인 (추정 산식)
     ⚠ 정확한 자격/혜택은 신청 URL 확인 필요

  ③ 청년 전세보증금 대출 [부분 매칭]
     ...

  ─────────────────────────────────
  종합:
    매칭 정책 3건
    월 절감 추정 40만원
    사용자 예산(70만원) 대비 57%

  ※ 중복 수혜 제한 정책 포함:
     - 서울시 청년월세 지원 vs 행복주택 (택1 가능성)
     → 신청 시 정책 URL에서 확인하세요.
```

---

## 3. 사용 가능한 신뢰 근거 정리

### 3-1. 명확한 근거 (코드/메모/발표에 인용 가능)

| 영역 | 근거 |
|---|---|
| 환산보증금 산식 | 주택임대차보호법 제7조의2 / 시행령 제9조 |
| 전월세 전환율 | 한국부동산원 자치구별 통계 (월간 공표) |
| 다기준 의사결정 | TOPSIS (Hwang & Yoon 1981, 학술 검증) |
| Cold-start 추천 | Housing.com RE-RecSys (ACM 2024) |
| Multi-resolution geo | Geo-Embeddings for Housing Search (2024) |
| 정책 자격 데이터 | 온통청년 API 공식 필드 (`sprtTrgtMinAge` 등) |
| 시세 데이터 | 국토부 실거래가 신고 (법적 신뢰성) |

### 3-2. 임의값 → 근거 보강 가능

| 영역 | 현재 | 보강 방안 |
|---|---|---|
| 보증금 → 월 환산 | `/200` 임의 | 한국부동산원 자치구별 전환율 |
| 점수 만점 기준 | 50만원 = 100점 | 사용자 예산 대비 비율 |
| 일회성 지원 환산 | 12개월 가정 | 명시 상수 + "추정" 표기 |

### 3-3. 솔직히 임의값 (보강 불가)

| 영역 | 한계 | 대응 |
|---|---|---|
| 정책 유형 간 점수 단일화 | 학술 표준 없음 | "추정 산식, 동네 간 상대 비교용" 명시 |
| 보증금 무이자 대출의 가치 환산 | 기회비용 계산법 다양 | 가정값 명시 + 사용자 확인 안내 |
| 정책 우선순위 | 사용자별 다름 | 사용자가 정렬 기준 선택 |

---

## 4. 구현 작업 단위 (Phase 6 분할)

### Phase 6-1: 자격 검증 흐름 재설계 (1.5일)
- `collect_youth_info()` 확장 — 소득/자산/혼인/무주택/학력 추가
- `_check_income()`, `_check_marriage()`, `_check_education()` 함수 추가
- 동네별 정책 후보 → 사용자 선택 흐름 변경
- 출력 포맷: "✅ 자동매칭 / ⚠ 사용자 확인" 표기

### Phase 6-2: 전월세 전환율 법정 산식 적용 (0.5일)
- 한국부동산원 자치구별 전환율 데이터 확보 (CSV 다운로드 or API)
- `conversion_rate_lookup(gu_name)` 함수 추가
- `analyze_benefit()` 내 보증금 환산 로직 변경
- 코드 주석에 근거(주택임대차보호법) 명시

### Phase 6-3: 정책 유형별 산식 분리 (1일)
- `BENEFIT_TYPE_FORMULAS` 딕셔너리 정의
- 유형별 산식 + 근거 + "추정 여부" 플래그
- 가정값 상수 분리 (`ASSUMED_RESIDENCE_MONTHS` 등)
- 코드 주석에 근거 명시

### Phase 6-4: 점수 산식 변경 (0.5일)
- `policy_selection_flow()` 점수 계산 부분 수정
- 사용자 예산 대비 비율 적용
- TOPSIS 통합 시 정규화된 값 전달

### Phase 6-5: 중복 수혜 키워드 검색 + 경고 (0.5일)
- `detect_dup_limit()` 함수 추가
- 결과 출력에 경고 표시
- 마지막 안내 메시지 추가

### Phase 6-6: 추천 근거 출력 포맷 강화 (1일)
- `print_policy_section()` 출력 변경
- 자격 검증 결과 표시
- 산출 근거 + 신뢰도 표시
- 중복 수혜 경고 통합

**총 작업: 약 5일** (Phase 6-1~6-6 순차 진행)

---

## 5. 발표 자료 활용 가능 핵심 메시지

### 5-1. 차별화 포인트
> "서울살이는 청년에게 단순 정책 목록이 아닌, 자격 자동 매칭 + 보조금 정밀 환산 + 신뢰도 표시까지 제공합니다. 환산보증금은 주택임대차보호법 법정 산식과 한국부동산원 자치구별 전환율을 적용했습니다."

### 5-2. 한계 솔직 인정
> "보조금 절감액 추정은 정책 유형별 산식이 학술적으로 표준화되지 않은 영역입니다. 본 시스템은 동네 간 상대 비교용 추정값을 제공하며, 정확한 혜택은 각 정책 신청 URL에서 사용자가 직접 확인하도록 안내합니다."

### 5-3. 신뢰성 보강 근거
- 법적: 주택임대차보호법
- 공공: 한국부동산원, 온통청년 API, 국토부 실거래가
- 학술: TOPSIS, Housing.com RE-RecSys, Multi-resolution geo

---

## 6. 사용자와의 결정 사항 요약

| 사항 | 결정 |
|---|---|
| 자격 검증 방식 | 동네 가능 정책 → 사용자 선택 → 자격 확인 |
| 공통/개별 자격 | 공통 자동 매칭, 개별 사용자 자가 확인 |
| 산식 출처 | 가능한 부분은 법정 산식/공공 통계 적용, 임의 부분은 명시 |
| 중복 수혜 | 자동 검출 불가 → 키워드 경고 + 사용자 확인 안내 |
| 점수 신뢰성 | 절대값 → 사용자 예산 대비 비율 + 상대 순위 |
| 추천 근거 | 매칭 사유, 자격 결과, 산출식, 신뢰도 모두 출력 |

---

## 7. 다음 단계 (Claude Code에서)

이 메모를 Claude Code 입력으로:
```
청년정책_시스템_개선계획.md를 읽고
Phase 6-1부터 순서대로 진행해줘.
각 작업 단위 완료 후 나에게 검증 요청하고,
큰 변경(파일 삭제/함수 제거 등)은 적용 전 미리 확인받아.
```

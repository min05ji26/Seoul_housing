# vibe 매핑 시스템 설계

작성일: 2026-05-04
대상 모듈: `vibe_module.py` (신규 생성)
적용 시점: Phase 6-2

---

## 1. 목적

사용자가 자연어로 표현한 동네 분위기 선호("조용한 곳", "번화한 동네" 등)를 시스템이 정량적으로 처리할 수 있는 형태로 변환하여, v5의 인프라 점수 계산에 반영한다.

---

## 2. 처리 흐름 (3단계)

```
[1단계] 사용자 자연어 → vibe 카테고리 매핑
        담당: LLM (Gemini/Claude API)
        입력: "조용한 동네", "한적한 곳" 등
        출력: 8개 카테고리 중 1개 또는 다중 선택, 매칭 실패 시 null

[2단계] vibe 카테고리 → 인프라 7종 가중치 변환
        담당: Python (vibe_module.py)
        입력: vibe 카테고리
        출력: 편의점/병원/약국/마트/카페/헬스장/공원 가중치

[3단계] 가중치 → v5 인프라 점수 계산
        담당: housing_recommendation_v5.py 수정
        v5 기존 로직에 가중치 적용
```

---

## 3. vibe 카테고리 (8개 + 폴백)

| 카테고리 | 매칭 표현 예시 | 핵심 인프라 |
|---|---|---|
| 조용함 | 조용한, 한적한, 고즈넉한, 사람 적은, 차분한 | 공원·병원 ↑ / 카페·헬스장 ↓ |
| 번화함 | 번화한, 활기찬, 시끌벅적, 사람 많은 | 카페·헬스장·편의점 ↑ |
| 청년활기 | 젊은 사람 많은, 20대, 활기찬 | 카페·헬스장 ↑↑ |
| 가족친화 | 가족, 아이 키우기 좋은, 안정적 | 병원·약국·마트 ↑ |
| 자연친화 | 공원 가까운, 녹지, 산책 | 공원 ↑↑↑ |
| 편의 우선 | 마트·편의점 가까운, 생필품 편한 | 편의점·마트 ↑ |
| 운동·건강 | 헬스장 가까운, 운동 시설 많은 | 헬스장·공원 ↑ |
| 카페·문화 | 카페 많은, 디저트, 문화 공간 | 카페 ↑↑ |
| (매칭 실패) | 역세권, 학세권, 치안, 신축 등 | null → 사용자 재질문 |

---

## 4. LLM 프롬프트 핵심

사용자 발화에서 분위기 표현을 8개 카테고리로 분류. 복합 표현(예: "조용하고 카페 많은")은 다중 매칭 가능. 8개에 명확히 매칭 안 되는 표현은 null로 처리하고 원본 표현을 보존하여 사용자에게 재질문.

---

## 5. 매칭 실패 처리

### 케이스 1. 매칭 가능 (대부분)
사용자 입력에서 8개 카테고리 중 하나 이상 매칭 → 가중치 적용 후 추천 진행

### 케이스 2. 매칭 어려움 (일부)
"역세권" 등 v5가 측정 못 하는 표현 → 챗봇이 사용자에게 재질문하여 8개 중 선택 또는 분위기 미반영 선택

### 케이스 3. 사용자 입력 없음
필수 슬롯 모두 채운 후 챗봇이 8개 옵션 + 균등 옵션 제시

---

## 6. 인프라 가중치 표

### 평가 척도
1~5 정수 (1=매우 부적합, 5=매우 적합)
1인 평가자 직관 기반. Jacobs(1961) 도시활력 이론과 한국 주거지 평가 연구 참고. 향후 사용자 피드백 누적 시 AHP 보정 예정.

### 가중치 표 (1~5 척도)

| vibe | 편의점 | 병원 | 약국 | 마트 | 카페 | 헬스장 | 공원 |
|---|---|---|---|---|---|---|---|
| 조용함 | 3 | 3 | 3 | 3 | 1 | 2 | 5 |
| 번화함 | 4 | 2 | 2 | 3 | 5 | 4 | 2 |
| 청년활기 | 4 | 2 | 2 | 3 | 5 | 5 | 3 |
| 가족친화 | 3 | 5 | 4 | 5 | 2 | 3 | 4 |
| 자연친화 | 3 | 3 | 3 | 3 | 2 | 3 | 5 |
| 편의 우선 | 5 | 3 | 3 | 5 | 3 | 2 | 2 |
| 운동·건강 | 3 | 3 | 3 | 3 | 2 | 5 | 5 |
| 카페·문화 | 3 | 2 | 2 | 3 | 5 | 3 | 3 |

### 정규화
1~5 점수 → 합계가 7이 되도록 정규화하여 가중치로 사용. 다중 vibe 시 각 카테고리 가중치 평균.

---

## 7. v5 통합

v5의 인프라 점수 계산 부분에 vibe 가중치 곱셈 적용. 인프라 7종 거리 점수에 카테고리별 가중치를 곱하여 가중 평균 산출.

---

## 8. 다른 안과의 비교 — 임베딩 검색 시스템

### 임베딩 검색이란
2026-04-27 메모에서 검토된 별도 안. 자연어 입력 전체를 768차원 좌표로 변환하고, 매물별로 사전 생성한 자연어 문단의 좌표와 코사인 유사도 매칭하는 방식.

### 두 시스템 비교

| 항목 | vibe 시스템 | 임베딩 검색 시스템 |
|---|---|---|
| 매물 데이터 | v5 인프라 7종 그대로 활용 | 매물별 자연어 문단 사전 생성 필요 |
| 매칭 방식 | 8개 카테고리 + 가중치 표 | 768차원 코사인 유사도 |
| 추천 로직 | v5 1·2·3단계 그대로 | 1·2단계 → 임베딩 → TOPSIS 통합 |
| 결과 설명 | 가중치 변경 명시 가능 | 유사도 숫자 + 키워드 노출 보완 |
| 검증 가능성 | 화이트박스 | 블랙박스 |
| 작업량 | 2~3일 | 12일 |
| 마감 적합성 | 적합 | 빡빡 |

---

## 9. vibe 시스템이 더 나은 이유

### 이유 1. 검증 가능성 (가장 중요)

임베딩 검색의 가장 치명적 한계는 **블랙박스**다.
- 사용자가 "왜 이게 추천됐어?"라고 물으면 "768차원 공간에서 거리가 가까웠다"는 답밖에 못 함
- SeoulHouse2Vec 같은 기존 시스템 비판할 때 지적했던 약점을 우리 시스템이 답습하는 자기모순 발생

vibe 시스템은 화이트박스다.
- "조용함 vibe → 카페 가중치 1, 공원 가중치 5"가 표로 명시
- 사용자에게 "이 동네는 공원 300m로 가깝고 카페 적어서 점수 높음"이라고 명확히 설명 가능
- 발표 시 심사위원 질문에도 가중치 표 보여주면 끝

### 이유 2. 데이터 보강 부담 없음

임베딩 검색은 매물별 자연어 문단을 사전 생성해야 한다.
- 매물 1만 개면 LLM 호출 1만 회 또는 규칙 기반 템플릿 처리 필요
- 카카오 Local API + 동별 통계 + 규칙 결합으로 가능하지만 시간 소요

vibe 시스템은 v5 기존 데이터(인프라 7종 거리)를 그대로 사용한다. 추가 데이터 보강 작업 없음.

### 이유 3. 작업량 차이

- 임베딩 검색: 12일 (LLM 파싱 + 매물 문단 생성 + 임베딩 + 통합 + 출력)
- vibe 시스템: 2~3일 (가중치 표 + 카테고리 매핑 + v5 통합)

마감 1.5주 안에 챗봇/Streamlit/UI까지 만들어야 하는 상황에서 작업량 차이가 결정적이다.

### 이유 4. 학술적 정당성

- vibe 가중치는 1~5 척도 명시 + Jacobs(1961) 도시활력 이론 인용 + AHP-TOPSIS 방법론 참고로 정당화 가능
- 임베딩 검색은 sentence-transformers 표준 라이브러리 사용으로 학술적 차별성이 약함
- 경진대회 창업부문에서 "독창적 처리 방식"으로 어필하기엔 vibe 쪽이 더 명확

### 이유 5. 한계가 명확한 곳에 한정됨

vibe 시스템의 한계는 8개 카테고리에 매칭 안 되는 표현(역세권, 학세권 등) 처리 불가.
하지만 이건 **사용자에게 솔직하게 재질문**하면 해결됨.

임베딩 검색의 한계는 **추천 결과를 신뢰할 근거 자체가 약하다는 본질적 문제**.
출력 단계에서 키워드 노출, LLM 설명 등으로 부분 완화는 가능하지만 본질은 여전히 블랙박스.

### 종합

vibe 시스템은 **임베딩 검색이 가졌던 강점(자연어 분위기 표현 흡수)을 부분적으로 유지하면서, 가장 치명적이었던 한계(블랙박스 + 데이터 보강 부담 + 작업량)를 모두 회피**한 해결책이다.

미묘한 라이프스타일 표현(예: "주말에 책 읽기 좋은 카페가 많고 평일엔 조용한") 같은 복합 미묘 표현은 vibe로 완전히 흡수 못 하는 한계는 인정. 다만 이 한계는 8개 카테고리의 다중 매칭으로 70~80%는 흡수 가능하며, 남은 표현은 매칭 실패 처리로 정직하게 대응한다.

---

## 10. 학술적 정당성

### 명시 가능한 근거
- Jacobs (1961) "The Death and Life of Great American Cities" — 도시활력 = 시설 다양성 + 사람
- 조월·이수기 (2021) "서울시 POI 빅데이터를 활용한 도시활력과 영향요인 분석" (KCI)
- AHP-TOPSIS 결합 — Sustainability 2025 한국 주거단지 재개발 평가 (방법론 인용)

### 솔직한 한계 인정
- 가중치 1~5 척도는 1인 평가자 직관 (전문가 합의 X)
- 향후 사용자 피드백 누적 시 정식 AHP로 보정 예정
- 절대값보다 동네 간 상대 비교 용도

---

## 11. 발표 시 메시지

> "동네 분위기 선호는 8개 카테고리로 정량화. 사용자 자연어 입력에서 LLM이 카테고리 자동 분류, 매칭 실패 시 사용자 재질문으로 잘못된 자동 매핑 방지. Jacobs(1961) 도시활력 이론과 AHP-TOPSIS 방법론 참고하여 1~5점 척도로 평가. 임베딩 검색의 블랙박스 한계를 회피한 화이트박스 방식으로, 추천 근거를 사용자에게 명확히 설명 가능."

# 이따 진행해야 할 사항

작성: 2026-05-05 (데스크탑 세션 종료 전)
다음 작업 환경: 노트북 (JangKyoungJun)

---

## ✅ 오늘 완료된 작업 (재작업 금지)

- Fix 1~3, 5: 챗봇 로직 버그 수정 (vibe 파싱, 주소 검증, 직접설정 가중치)
- 추가 버그 6가지 수정 (vibe 버튼 9개, 월세 순서, 보증금/통근시간 파싱, CSV 패턴, geocode fallback)
- Claude_Code_인수인계.md 갱신 완료
- git 커밋 + push 완료 (커밋: 3410758)

---

## 🔧 다음에 진행할 작업: 프로젝트 폴더 구조 정리

### 목표 구조

```
서울살이_프로젝트/
├── docs/
│   ├── handover/       ← 인수인계 MD
│   ├── design/         ← 설계·계획 MD
│   └── notes/          ← 메모·분석 MD
├── core/               ← 추천 엔진 Python
├── chatbot/            ← 챗봇·NLP Python + prompts/
├── webapp/             ← 웹앱 (현재 구조 유지)
├── data/               ← CSV (사용_csv_모음 → data)
├── tests/              ← test_v5_auto.py
├── logs/               ← scenario*.log
├── results/            ← api코드결과/
└── images/
```

### 파일 이동 계획

#### docs/handover/
- Claude_Code_인수인계.md
- Claude_Code_인수인계_추가.md
- 프로젝트_인수인계_메모.md

#### docs/design/
- phase7_챗봇_웹앱_설계.md
- vibe_매핑_설계.md
- 청년정책_시스템_개선계획.md
- 프로젝트_변경_계획.md
- 회원가입_설계.md
- Claude_Code_작업지시.md

#### docs/notes/
- v5_향후_보완사항.md
- phase6_46_미실행_원인_메모.md
- 주거비_데이터_정보_메모.md
- 프로젝트_분석정보_메모.md
- PPT_제작_프롬프트.md
- PPT_키워드_설명자료.md

#### core/
- housing_recommendation_v5.py
- vibe_module.py
- youth_policy_module.py
- feedback_module.py
- job_search_module.py

#### chatbot/
- nlp_input_module.py
- llm_module.py
- prompts/ (폴더째 이동)

#### 기타
- 사용_csv_모음/ → data/
- api코드결과/ → results/
- scenario*.log → logs/
- test_v5_auto.py → tests/

#### 삭제
- Claude_Code_인수인계 - 복사본.md (중복)

---

### ⚠️ Python 파일 이동 시 반드시 수정해야 할 import 목록

파일을 옮기면 import가 전부 깨짐. 아래 수정 필수:

| 파일 | 수정 내용 |
|------|-----------|
| `webapp/main.py` | `sys.path`에 `core/`, `chatbot/` 경로 추가 |
| `chatbot/nlp_input_module.py` | `from vibe_module` → `from core.vibe_module` |
| `chatbot/nlp_input_module.py` | `from llm_module` → `from chatbot.llm_module` |
| `chatbot/llm_module.py` | `PROMPTS_DIR` 경로 (`__file__` 기준 재계산) |
| `webapp/main.py` | CSV glob 경로 `사용_csv_모음` → `data` |
| `core/housing_recommendation_v5.py` | 내부 경로 변수 점검 |
| `core/youth_policy_module.py` | 내부 경로 변수 점검 |
| `tests/test_v5_auto.py` | import 경로 전부 점검 |

### 작업 순서 (Claude Code에게 지시)

1. 폴더 생성 (docs/handover, docs/design, docs/notes, core, chatbot, data, tests, logs, results)
2. MD 파일 이동 (import 영향 없음, 먼저 처리)
3. 비코드 파일 이동 (logs, results, tests, data)
4. Python 파일 이동 + import 수정 동시 진행
5. 서버 재시작 후 동작 확인 (사용자 직접 테스트)
6. 정상 확인 후 git 커밋 + 인수인계 갱신

---

## 📋 이후 추가 작업 (폴더 정리 완료 후)

- 사용자 테스트 중 발견된 추가 오류 있으면 처리
- 경진대회 마감 2026-05-13 18:00 전 체크리스트:
  - [ ] 모바일 반응형 최종 확인
  - [ ] 추천 결과 UI 마무리
  - [ ] 외부 시연 준비 (Cloudflare Tunnel 등)

---

## 🖥️ 노트북 작업 시작 전 필수

```powershell
# 1. git pull
git pull origin master

# 2. .env 파일 확인 (노트북에 없으면 별도 복사 필요)
# 키 목록: KAKAO_LOCAL_REST_API_KEY, KAKAO_MOBILITY_REST_API_KEY,
#          ODSAY_API_KEY, MOLIT_API_KEY, YOUTH_API_KEY,
#          SEOUL_JOB_API_KEY, GEMINI_API_KEY

# 3. 서버 실행
python -m uvicorn webapp.main:app --host 0.0.0.0 --port 8000
```

# 다음 세션 이어할 작업

작성: 2026-05-05 (2차 버그 수정 세션 종료 후)
마감: **2026-05-13 18:00** (경진대회 접수)

---

## ✅ 이번 세션까지 완료된 것 (재작업 금지)

- `_df_to_list()` 컬럼명 전면 수정 → 추천 결과 점수/가격/통근시간 정상 출력
- `youth_policy_module._TEST_MODE = True` 강제 설정 제거 → 실제 청년정책 API 호출 정상화
- LLM 추출 `work_address` 검증 → 역명(강남역 등) 입력 시 재질문 정상 동작
- 통근시간 숫자만 입력("80") → 80분으로 인식
- git 커밋 완료

---

## 🔧 미완료 작업 목록 (우선순위 순)

### 1순위 — 사용자 테스트 중 발견된 버그 처리
> 테스트하다 새 오류 나오면 그때그때 처리. 아래 항목보다 우선.

---

### 2순위 — 폴더 구조 정리
> `이따진행해야할사항.md` 에 상세 계획 있음. 참고해서 진행.

**목표 구조**:
```
서울살이_프로젝트/
├── docs/handover/   ← 인수인계 MD
├── docs/design/     ← 설계·계획 MD
├── docs/notes/      ← 메모·분석 MD
├── core/            ← 추천 엔진 Python
├── chatbot/         ← 챗봇·NLP Python + prompts/
├── webapp/          ← 웹앱 (현재 구조 유지)
├── data/            ← CSV (사용_csv_모음 → data)
├── tests/           ← test_v5_auto.py
├── logs/            ← scenario*.log
└── results/         ← api코드결과/
```

**파일 이동 후 반드시 수정할 import**:
| 파일 | 수정 내용 |
|------|-----------|
| `webapp/main.py` | `sys.path`에 `core/`, `chatbot/` 경로 추가 |
| `chatbot/nlp_input_module.py` | `from vibe_module` → `from core.vibe_module` |
| `chatbot/nlp_input_module.py` | `from llm_module` → `from chatbot.llm_module` |
| `chatbot/llm_module.py` | `PROMPTS_DIR` 경로 (`__file__` 기준 재계산) |
| `webapp/main.py` | CSV glob 경로 `사용_csv_모음` → `data` |
| `core/housing_recommendation_v5.py` | 내부 경로 변수 점검 |
| `tests/test_v5_auto.py` | import 경로 전부 점검 |

**작업 순서**:
1. 폴더 생성
2. MD 파일 이동 (import 무관)
3. 비코드 파일 이동 (logs, results, tests, data)
4. Python 파일 이동 + import 수정 동시 진행
5. 서버 재시작 후 동작 확인

---

### 3순위 — 청년정책 흐름 개선 (Problem 4-2~4-4)

현재 상태: 청년정책 여부만 묻고 (`use_youth_policy`) 실제 API 호출 시 하드코딩된 더미 값 사용 중.

```python
# get_v5_params() 현재 코드 — user_info 전부 더미값
user_info = {
    "age": "29", "employment": "재직", "income_manwon": "250",
    "marriage": "미혼", "education": "대졸", "no_house": "무주택",
}
```

**4-2: 공통 자격 슬롯 7개 추가 + 순차 질문**
```
age (나이), employment (재직/구직/기타), income_manwon (월소득),
marriage (미혼/기혼), education (고졸/대졸/대학원), no_house (무주택 여부), region (서울 거주 여부)
```
- `use_youth_policy = True` 확인 후 위 7개 슬롯을 순서대로 질문
- `nlp_input_module.py` `_missing_required()` 또는 `_next_question()` 분기에서 처리
- `SLOT_QUESTIONS`에 7개 질문 텍스트 추가

**4-3: 실제 매칭 로직 연동**
- `get_v5_params()`의 `user_info` 딕셔너리를 실제 수집된 슬롯값으로 교체
- `youth_policy_module.py`의 `find_matching_policies(user_info)` 정상 호출되는지 확인

**4-4: 정책 카드 UI**
- 추천 결과 화면에서 정책 항목을 별도 카드 형태로 표시
- `webapp/static/app.js` 결과 렌더링 부분 수정
- 카드: 정책명 / 혜택 요약 / 월 절감액 / URL 버튼

---

### 4순위 — 오타·조사 처리 (Problem 1)

현재 상태: "강남역에서 일해요" 같은 조사 포함 발화를 LLM이 `강남역에서` 로 추출하는 경우 있음.

**방향**:
- `prompts/slot_extraction.txt` 업데이트: 조사·어미 제거 지시 추가
- `process()` 진입 시 간단한 regex pre-cleaning: `에서$|에서\s|으로$` 등 조사 제거

---

### 5순위 — 편도/왕복 기준 안내 명확화 (Problem 2)

현재 상태: v5 `allowed_commute_min`은 **편도** 기준. 사용자가 왕복 기준으로 입력할 수 있음.

**방향**:
- `SLOT_QUESTIONS["allowed_minutes"]` 텍스트에 "편도 기준" 명시
- `_parse_commute_minutes()` 이미 왕복 입력 감지 시 `//2` 처리함 → 동작 확인만 필요

---

### 6순위 — 경진대회 마감 전 체크리스트

- [ ] 모바일 반응형 최종 확인 (max-width 540px)
- [ ] 추천 결과 UI 완성도 확인 (점수·가격·통근 표시 정상 여부)
- [ ] 외부 시연 환경 구성 (Cloudflare Tunnel 또는 ngrok)
- [ ] `.env` 파일 노트북 동기화 확인

---

## 🖥️ 세션 시작 전 필수

```powershell
# 1. git pull
git pull origin master

# 2. 서버 실행 (--reload 절대 금지)
python -m uvicorn webapp.main:app --host 0.0.0.0 --port 8000

# 3. health check
curl http://localhost:8000/api/health
```

---

## 📌 파일 위치 참고

| 파일 | 역할 |
|------|------|
| `nlp_input_module.py` | 챗봇 슬롯 상태머신 (ChatBot 클래스) |
| `llm_module.py` | Gemini 슬롯 추출 + 폴백 |
| `webapp/main.py` | FastAPI 라우트 + `_df_to_list()` |
| `housing_recommendation_v5.py` | 3단계 추천 엔진 |
| `youth_policy_module.py` | 청년정책 매칭 |
| `vibe_module.py` | 동네 분위기 가중치 |
| `prompts/slot_extraction.txt` | Gemini 슬롯 추출 프롬프트 |
| `사용_csv_모음/주거비_데이터_최종통합버전.csv` | 주거비 데이터 (97MB) |
